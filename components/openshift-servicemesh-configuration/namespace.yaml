apiVersion: v1
kind: Namespace
metadata:
  labels:
    kubernetes.io/metadata.name: istio-system
    argocd.argoproj.io/managed-by: openshift-gitops
    topology.istio.io/network: network-${CLUSNER_NAME}

  name: istio-system