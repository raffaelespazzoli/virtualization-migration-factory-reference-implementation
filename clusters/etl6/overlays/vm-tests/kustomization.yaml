apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vm-tests

resources:
  - namespace.yaml
  # fedora-vm1 / fedora-vm2 retired; YAML kept in-tree if we need them again
  # - fedora-vms.yaml
  - ssh-key-secret.yaml
