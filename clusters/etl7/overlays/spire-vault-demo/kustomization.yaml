apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spire-vault-demo

resources:
  - namespace.yaml
  - cert-bootstrap.yaml
  - virtual-machine.yaml
  - service.yaml
  - route.yaml
  - spire-registration-job.yaml
